# 更新日志

## [1.0.5] - 2026-09-01

### ⚠️ 重要变更：改名为 ZM-QQGroupmgr

`QQManager` 字面是「QQ 管理器」，没体现「群」这层含义；「群管」的英文惯用写法是
`group manager` / `group admin`，因此本版起统一改名：

- 展示名 `display_name`：`ZM-QQManager` → **`ZM-QQGroupmgr`**
- 插件标识符 `name`：`astrbot_plugin_zm_qqmanager` → **`astrbot_plugin_zm_qqgroupmgr`**
- GitHub 仓库：`astrbot_plugin_zm_qqmanager` → **`astrbot_plugin_zm_qqgroupmgr`**
  （旧地址由 GitHub 自动跳转，已有 clone 仍可用）

> **升级提示**: 插件标识符变了，AstrBot 会把它识别为一个新插件，升级后请到插件
> 配置面板确认一次配置项（`_conf_schema.json` 里的配置不会自动带过去）。
>
> **运行数据不用手动搬**：封禁列表、各功能开关、自定义敏感词、广告与迎送文本、
> 文件仓库与更新日志所在的数据目录 `data/plugin_data/ZM-QQManager` 会在插件首次
> 加载时自动改名为 `data/plugin_data/ZM-QQGroupmgr`，数据原样保留。若两个目录同时
> 存在，插件用新目录、不动旧目录；改名失败（如被占用）则继续用旧目录并打日志。

### ✨ 新增功能

**`/zmhelp` 菜单内容可自定义**
- 新增插件配置 `help_menu_text`（多行文本）：留空沿用内置的完整菜单，
  填写后 `/zmhelp` 只显示你写的内容
- 支持三个占位符：`{name}` 插件名、`{version}` 版本号、`{cooldown}` 当前下载冷却

### 🔧 改进

**数据目录自动迁移**
- `core/store.py` 新增 `LEGACY_DIR_NAMES`，插件取数据目录时若只有旧目录，
  整目录改名为新名字；新目录已存在则不动旧目录，改名失败则回落旧目录继续跑
- 文件仓库条目 `files.json` 里存的是上传当时的**绝对路径**，目录改名后会集体
  失效。新增 `FileRepository._entry_path()`，原路径不存在时按文件名回落到当前
  仓库目录（回落同样做目录内校验），`delete` / `issue_token` / `resolve_token`
  三处统一走它。整个 AstrBot 目录被搬走的情况顺带一起修好了
- 新增 `test_plugin.py` 自检脚本（`python test_plugin.py`，不依赖 pytest），
  覆盖帮助菜单三条分支、文件路径回落四种情况、数据目录迁移四种情况

**`/zmhelp` 合并转发卡片标题**
- 之前帮助是纯文本，超长后由 AstrBot 自动转合并转发，卡片标题只能是协议端
  默认的「群聊的聊天记录」
- 现在插件自己发合并转发，卡片标题固定为 **`ZM-QQGroupmgr v<版本号>`**；
  协议端不支持自定义卡片或在私聊使用时，仍回落为纯文本

**版本号单点维护**
- 新增 `PLUGIN_NAME` / `PLUGIN_VERSION` 常量，`@register`、启动日志、帮助标题
  统一取值，不再三处各写一遍

### 📝 文档

- README 与插件市场介绍文档（`metadata.yaml` 的 `desc`）统一改为同一套排版风格
- 修正 README 里「`/zmhelp` 对全体成员开放」的错误说明（该指令实际仅管理员可用）
- 补齐 README 配置项表格中遗漏的 `file_download_cooldown`、`file_cooldown_tip`

### 🗑️ 清理：删掉 7 份过期文档

v1.0.0 / v1.0.1 时期的一次性开发文档全部删除。它们的内容已经全面失效，同时存在
只会让人分不清哪份是当前的说明：

| 删除的文件 | 失效点 |
| :--- | :--- |
| `COMPLETION_REPORT.txt` | v1.0.0 构建报告，「727 行 / 23 个命令」（现为 3000+ 行 / 26 组指令） |
| `INSTALLATION.txt` | 写着作者本机路径，「已创建 5 个提交 / 标签 v1.0.0」 |
| `PROJECT_SUMMARY.md` | v1.0.0 的 9 文件目录树；`zm_qqmanager_*` KV 键名从未在现版本存在 |
| `GITHUB_GUIDE.md` | 教人创建名为 `ZM-QQGroupmgr` 的仓库，仓库名早已不是这个 |
| `RELEASE_CHECKLIST.md` | v1.0.1 清单，`main.py 28KB`（现为 122KB） |
| `QUICKSTART.md` | 与 README「快速开始」重复，且插件目录写成 `astrbot/plugins`（应为 `data/plugins`） |
| `DEPLOY.md` | 插件目录写错；称数据存 AstrBot KV（实为插件目录下 JSON 文件）；`self.max_history_per_group` 这个属性不存在；称撤回有 2 分钟时限（管理员撤他人消息无时限）；称自定义广告规则「未来版本支持」（`ad_threshold` 早已可调） |

安装、权限、指令、配置项、常见问题、安全提醒现在只有 **README.md** 一处说明，
变更记录只有 **CHANGELOG.md** 一处。顺带删掉仓库根目录里遗留的空 `files/`
目录（文件仓库实际落在 `data/plugin_data/ZM-QQGroupmgr/files/`）。

---

## [1.0.4] - 2026-09-01

### ⚠️ 重要变更：插件标识符与仓库名

为通过 AstrBot 插件市场校验，插件标识符与仓库名改为符合规范的写法：

- `metadata.yaml` 的 `name` 由 `ZM_QQManager` 改为 `astrbot_plugin_zm_qqmanager`
- GitHub 仓库由 `ZM-QQManager` 重命名为 `astrbot_plugin_zm_qqmanager`
  （旧地址由 GitHub 自动跳转，已有 clone 仍可用）
- 展示名 `display_name` 保持 `ZM-QQManager` 不变

> **升级提示**: 插件标识符变了，AstrBot 会把它识别为一个新插件。
> 升级后请到插件配置面板确认一次配置项；按群保存的运行数据
> （封禁列表、开关、敏感词、文件仓库等）存放位置未变，不受影响。

### ✨ 新增功能

**群资料管理 `/group`（缩写 `/g`）**
- `/group newname <文本> [qq群号]`（缩写 `/g nn`）修改群名称
- `/group pp [图片] [qq群号]`（缩写 `/g avatar`）修改群头像
- `[qq群号]` 省略时作用于当前群；用 `-` 连接可批量操作，
  如 `/g pp 3366-1009-10032` 给三个群换同一张头像（单次上限 20 个群）
- 换头像时图片可随命令附带（电脑端直接粘贴），也可在机器人提示
  「1分钟内 请发送图片 否则上传失败」后补发；超时则指令失效、执行失败
- 上传成功后若机器人是群主或管理员，自动撤回那张图片消息，
  可用配置 `avatar_recall` 关闭（默认开启）
- 群名以数字结尾时用引号包住：`/g nn "老年活动中心 2025"`

**群公告 `/broadcast`（缩写 `/bc`）**
- `/broadcast <内容> <是否置顶公告>`，第二个参数只能填 `true` 或 `false`
- 内容里可以直接插入图片（电脑端粘贴），一次执行完成
- 只发了文字时机器人追问「是否需要上传图片？」，回答 `是` 则等待 1 分钟内
  发送图片后发布带图公告，回答 `否` 则直接发布纯文本公告
- 带图公告发布后按配置 `broadcast_recall` 撤回图片（默认开启）
- 置顶依赖协议端支持 `pinned`；协议端不支持时公告照发并明确提示未置顶

**退群提示 `/bye`**
- 监听 `group_decrease` 事件，成员退群后发送自定义内容
- `/bye set <文本>` | `image` | `image clear` | `on` | `off` | `reset` | `status`
- 自定义内容可附带图片（`/bye image`，图片长期保存在插件数据目录）
- 区分主动退群与被踢：占位符 `{reason}`（"退出了本群" / "被移出群聊"）、
  `{operator}`（执行踢出的管理员）
- 机器人自己被踢出群时不发送提示

### 🔧 新增配置

- `media_wait_timeout`: 等待补发图片 / 答复的秒数，默认 60
- `avatar_recall`: 换群头像后是否撤回图片消息，默认开启
- `broadcast_recall`: 带图公告发布后是否撤回图片消息，默认开启
- `farewell_recall`: 设置退群提示图后是否撤回图片消息，默认开启

### 🐛 问题修复

- **协议端返回 null 被误判为失败**: `delete_msg`、`delete_group_file`、
  `_send_group_notice`、`set_group_portrait` 这类动作成功时 `data` 本来就是
  null，原先用「返回值非 None」判断成败，会把成功当成失败。典型表现是
  `/file upload` 明明删掉了群文件，却回复「上传失败：删除群文件失败」。
  新增 `OneBotApi.try_ok()` 按是否抛异常判断成败，相关调用点已全部改用它。
- **入群欢迎的昵称未转义**: `{name}` 现在填入真实昵称（原先填的是 QQ 号），
  并对昵称做 CQ 码转义——否则一个昵称叫 `[CQ:at,qq=all]` 的成员进群，
  就能借机器人之口 @ 全体成员。
- **相对数据目录导致换头像崩溃**: 老版本 AstrBot 取不到数据目录时会退回相对
  路径 `data/`，而 `Path.as_uri()` 对相对路径抛 `ValueError`，会让整条换头像
  指令异常退出。临时图片改为写入绝对路径，并对 `as_uri()` 做兜底。

### 🛡️ 健壮性

- 图片按魔数校验格式（jpg / png / gif / bmp / webp），挡住把其他文件当图片发
- 图片大小上限 10MB，下载时边下边计字节数，避免超大响应打满内存
- `file` 字段依次尝试 base64 / `file://` / 裸路径，兼容支持度不同的协议端
- 两步交互的等待状态带 token 校验：等待期内重新执行指令，旧的超时任务不会
  误杀新登记；同时挂起数量上限 200，防止字典被反复触发的指令撑大
- 两步交互只认严格相等的「是」「否」，等待期内的其他指令一律放行；带图片的
  指令调用也会放行给指令本身处理，不会被旧登记按旧参数抢走
- 群号用作文件名前剥掉非数字字符，杜绝路径穿越
- 批量操作每个群之间间隔 0.5 秒，避免被协议端限流

## [1.0.3] - 2026-08-29

### ✨ 新增功能

**下载冷却**
- 同一成员两次获取下载链接之间需等待冷却，默认 5 分钟（管理员不受限）
- 新增配置 `file_download_cooldown`（秒，填 0 关闭）与 `file_cooldown_tip`
- 新增 `/file download cdreset <成员>`（简写 `/f dl cdreset`）重置成员冷却，仅管理员可用

**文件指令补全**
- `/file log` 可简写为 `/f l`，`/file log update` 可简写为 `/f l up`
- 下载链接默认有效期由 3600 秒改为 600 秒
- `/f l <目标文件> <次数>` 的合并转发卡片名称与目标文件保持一致
- `/zmhelp` 补全全部命令缩写

**下载指令简写与私聊限制**
- `/file download` 可简写为 `/f dl`（另支持 `/f up`、`/f ls`）
- 下载链接只在私聊下发；群内执行下载指令时机器人只回复提示语
- 新增配置 `file_private_only_tip` 自定义该提示语（默认「需要私聊才可以下载」）

**链接失效提示可自定义**
- 新增配置 `file_link_expired_tip`（默认「链接已失效」），
  链接过期、无效或文件已删除时网页都显示该提示语

**批量撤回重写**
- `/recall <数量>` 改为优先向协议端拉取群历史记录（`get_group_msg_history`），
  不再只依赖插件内存记录，因此机器人自己发的消息、插件启动前的消息都能撤回
- 逐条记录撤回结果并汇报：`撤回完成：成功 X 条，失败 Y 条（共 Z 条）`，
  失败条目附带消息 ID、发送者与协议端返回的原因（最多列 5 条，其余记入日志）
- 引用回复撤回成功后会顺带撤回 `/recall` 指令本身

### 🔒 安全与健壮性

- **CQ 码注入**：昵称、群名片、敏感词等成员可控内容会被拼进机器人要发送的
  文本消息里，此前未转义，成员把名片改成 `[CQ:at,qq=all]` 即可借机器人之口
  执行 CQ 码。现在除插件自己生成的 `{at}` 外，所有填入值一律转义 `[` `]` `&`
- **上传扩展名目录穿越**：扩展名此前直接取自上传方提供的文件名，
  形如 `x.zip\..\..\evil` 的名字在 Windows 上会把文件写到仓库目录之外。
  现在扩展名只放行「点 + 1~10 位字母数字」，并额外校验落点仍在仓库目录内
- **下载响应头污染**：文件名中的控制字符（含 CR/LF）与引号现在会被清除，
  不再进入 `Content-Disposition`
- **数据丢失**：`JsonStore.save()` 此前在工作线程里序列化，若同时有协程改动
  同一个 dict，`json.dump` 会抛 `dictionary changed size during iteration`，
  整次保存静默失败。现在序列化在事件循环内完成；写失败也不再残留 `.tmp`
- 新增配置 `merge_admin_only`：`/merge` 能以任意 QQ 号伪造聊天记录，
  默认维持「全体可用」，公开群建议开启以防冒名
- 全体禁言的定时解除任务改为持有引用，避免被 GC 提前回收；插件卸载时取消
- `/file upload` 登记表会清理超时未发文件的记录，不再无限增长
- 下载链接若会生成 `http://0.0.0.0:端口`，启动时给出明确告警

### 🐛 Bug 修复

- **命令参数解析**：此前在整条消息里搜索命令名来剥离命令头，导致
  `/af on`、`/flood on`、`/cc on` 全部失效（识别不到 `antiflood`/`cardcheck`，
  参数变成 `["/af", "on"]`，一律走"查看状态"分支）；`/f dl guofile` 之类
  参数中含 "file" 的命令则会被整段截断成空参数。现在只比对消息的第一个 token
- 修复 `/recall 20` 实际只能撤回极少数消息的问题：插件内存记录只保存插件
  启动后收到的群消息，且从不包含机器人自己发出的消息，导致候选集远小于请求数量
- 撤回失败不再笼统地提示「超过 2 分钟撤回时限」——群管理员/群主撤回他人消息
  本就没有时限，该提示会误导排查方向
- 修复群内上传后自动清理群文件的逻辑完全失效的问题（`api` 变量未定义，
  异常被吞掉，实际从未执行撤回或删除）
- 上传清理规则改为按机器人身份判定：群主 → 撤回文件消息；
  群管理员 → 删除群文件；两者都不是 → 提示上传失败且不入库
- 删除群文件时兼容需要 `busid` 参数的协议端
- 先确保文件已落到本地再执行撤回/删除，避免文件被删后取不到本体

## [1.0.2] - 2026-08-29

### ✨ 新增功能

**指令简写与权限优化**
- `/file` 指令现在可以简写为 `/f`（如 `/f upload`、`/f download` 等）
- `/file delete` 可以简写为 `/f del`
- `/slimefinder` 指令现在所有人可用（之前仅管理员）
- `/merge` 指令现在所有人可用（之前仅管理员）

**消息撤回增强**
- `/recall <数量> [是否包含机器人]` - 新增可选参数控制是否撤回机器人自己的消息
- 默认包含机器人消息，可用 `/recall 5 false` 排除机器人消息

**文件上传优化**
- 管理员上传文件成功后自动清理群文件：
  - 机器人是群主时：撤回管理员发送的文件消息
  - 机器人是管理员时：直接删除群主或其他管理员上传的群文件
  - 机器人无权限时：不执行删除操作

### 🐛 Bug 修复

- 修复 `/merge` 指令中 title 显示错误的问题（之前显示"群聊的聊天记录"，现在正确显示用户输入的标题）

### 🔧 配置优化

- 优化文件上传相关配置项的描述，更加简单易懂：
  - `file_host` - 简化监听地址说明
  - `file_port` - 添加端口占用提示
  - `file_base_url` - 优化访问地址格式说明
  - `file_default_ttl` - 添加默认值说明

### 📝 文档更新

- 更新 `/zmhelp` 帮助信息，反映所有权限和功能变更
- 更新指令说明，标注哪些指令全体可用

---

## [1.0.1] - 2026-08-29

### ✨ 新增功能

**禁言增强**
- `/mutelist` - 查看本群禁言列表，显示每人剩余禁言时长
- `/muteall [时长]` - 全体禁言，支持 秒/分/时/天，不带参数默认永久
- `/muteall off` - 解除全体禁言

**敏感词系统**
- `/sw on [时长]` / `/sw off` - 按群开关，时长默认单位为分钟
- 修改时长需先 `/sw off` 再 `/sw on <新时长>`
- `/sw add <文本>` / `/sw del <文本>` - 管理自定义词（全称 `/sensitive-words`）
- `/sw mode custom|library|both` - 三种词源：仅自定义词、远程词库、混合
- `/sw list` / `/sw reload` - 查看词库状态、重新拉取远程词库
- 命中后自动撤回并禁言，支持白名单修正误伤

**刷屏检测**
- `/antiflood on|off` - 按群开关
- 滑动窗口条数判定 + 连续重复内容判定，阈值均可在后台配置

**群名片检测**
- `/cardcheck on|off` - 检测群名片中的广告与敏感内容
- 处理方式可配置：warn / reset / mute / kick

**文件仓库**
- `/file upload <name> <时长>` - 上传后随即发送文件即可入库
- `/file download <name>` - 获取限时临时下载链接
- `/file log <name> [次数]` - 以合并转发形式查看最近更新记录
- `/file log update <name> <版本> <说明>` - 记录版本与更新内容
- `/file list` / `/file delete <name>`

**其他**
- `/merge <标题> <QQ> <内容> [<QQ> <内容> ...]` - 构造合并转发消息
- `/kill <成员> <理由>` - 赛博击杀，向共同群同步播报，模板后台可改
- `/zmhelp` - 命令总览

### 🔧 变更

- 管理类命令统一限制为 AstrBot 管理员可用
- `/file download`、`/file list`、`/file log`（查看）对全体群成员开放，
  `/file upload`、`/file delete`、`/file log update` 仍限管理员
- 补全 `_conf_schema.json` 中缺失的 `ad_threshold`、`ad_mute_duration`、`ad_tip`
- 代码拆分为 `core/` 子模块：配置存储、时长解析、检测器、敏感词、文件仓库、下载服务

### ⚠️ 注意

- 内置下载服务不做身份校验，凭一次签发的限时令牌访问。
  公网暴露前请确认 `file_host` / `file_port` 的可访问范围。

---

## [1.0.0] - 2026-08-29

### 首次发布

#### ✨ 新增功能

**成员管理**
- 禁言管理：支持自定义时长（天/周/月/年）
- 踢出成员：支持踢出指定成员或清理不活跃用户
- 封禁系统：永久封禁用户，自动拒绝加群申请
- 管理员管理：设置或取消群管理员权限

**消息管理**
- 消息撤回：支持撤回指定消息或批量撤回
- 消息记录：自动记录群消息（每群最多100条）

**广告拦截**
- 智能广告检测：基于多维度评分机制
- 自动处理：检测到广告后自动撤回并禁言10分钟
- 支持手动开关广告拦截功能

**群公告与欢迎**
- 自定义群广告：保存和发布群内广告
- 入群欢迎：自动欢迎新成员，支持自定义文本
- 占位符支持：{at}、{name}、{user_id}、{group_id}

**特色工具**
- 群头衔管理：设置或取消群成员专属头衔
- 史莱姆区块查找器：我的世界史莱姆区块查找工具

#### 📝 命令列表

- `/mute` - 禁言用户
- `/kick` - 踢出成员或清理不活跃成员
- `/ban` - 封禁用户
- `/unban` - 解除封禁
- `/recall` - 撤回消息
- `/ad` - 广告管理
- `/adban` - 广告拦截开关
- `/wel` - 欢迎消息管理
- `/title` - 群头衔管理
- `/op` / `/deop` - 管理员管理
- `/slimefinder` / `/sf` - 史莱姆区块查找

#### 🔧 技术实现

- 使用 AstrBot Star API
- 基于 KV 数据库的持久化存储
- 完全异步设计
- 智能广告检测算法

---

## 未来计划

- [ ] 支持更多自定义广告检测规则
- [ ] 添加定时任务功能
- [ ] 群数据统计和分析
- [ ] 更多我的世界工具
- [ ] WebUI 配置界面
